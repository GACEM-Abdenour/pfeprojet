# Copier vers .env :  cp .env.example .env
# Mot de passe du compte SA de SQL Server (obligatoire pour Docker).
# Règles Microsoft : au moins 8 caractères, majuscule, minuscule, chiffre, symbole.
MSSQL_SA_PASSWORD=GiaDemo#2025Sql

# Ports (optionnel)
WEB_PORT=8080
SQL_PORT=1433
